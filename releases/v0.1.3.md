# v0.1.3

- Respect `X-OGM-Managed=false` as an explicit discovery opt-out: a desktop
  entry marked this way is excluded even when it matches a legacy desktop
  glob. distrobox-gaming uses it to keep the Steam launcher out of the grid.
