# v0.1.6

- Fix `ogm refresh` never downloading SteamGridDB covers for games known only
  via `X-OGM-*` desktop metadata (not in the bundled catalog): refresh now
  merges the marker overlay from a desktop scan, same as `ogm scan` does.
  Missing covers self-heal on the next refresh — which the overlay runs
  automatically on every launch.
